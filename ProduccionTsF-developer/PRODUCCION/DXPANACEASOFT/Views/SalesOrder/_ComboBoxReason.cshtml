@model DXPANACEASOFT.Models.DTOModel.SalesOrderDTO

@Html.DevExpress().ComboBoxFor(model => model.id_orderReason, settings =>
{
	settings.Name = "ComboBoxReason";
	settings.Width = Unit.Percentage(100);
	//settings.Properties.NullText = "Todos...";
	settings.CallbackRouteValues = new
	{
		Controller = "SalesOrder",
		Action = "ComboBoxReason",
	};
	settings.Properties.CallbackPageSize = 15;
	settings.Properties.TextField = "Text";
	settings.Properties.ValueField = "Value";
	settings.Properties.ClearButton.DisplayMode = ClearButtonDisplayMode.OnHover;
	settings.ReadOnly = !((bool)ViewBag.enabled);
}).BindList(ViewData["Reason"]).GetHtml()