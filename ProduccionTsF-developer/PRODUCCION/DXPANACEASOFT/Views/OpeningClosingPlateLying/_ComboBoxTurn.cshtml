@model DXPANACEASOFT.Models.DTOModel.OpeningClosingPlateLyingDTO

@Html.DevExpress().ComboBoxFor(model => model.id_turn, settings =>
{
    settings.Name = "ComboBoxTurn";
    settings.Width = Unit.Percentage(100);
    //settings.Properties.NullText = "Todos...";
    settings.CallbackRouteValues = new
    {
        Controller = "OpeningClosingPlateLying",
        Action = "ComboBoxTurn",
    };
    settings.Properties.ClientSideEvents.SelectedIndexChanged = "ComboBoxTurn_SelectedIndexChanged";
    settings.Properties.CallbackPageSize = 15;
    settings.Properties.TextField = "Text";
    settings.Properties.ValueField = "Value";
    settings.Properties.ClearButton.DisplayMode = ClearButtonDisplayMode.OnHover;
    //settings.Properties.ValidationSettings.RequiredField.IsRequired = true;
    settings.ReadOnly = !((bool)ViewBag.enabled);
}).BindList(ViewData["Turn"]).GetHtml()